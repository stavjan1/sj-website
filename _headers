# Cloudflare Pages headers — kill the Edge/Safari stale-cache problem.
# HTML must always revalidate (so a deploy shows up on the next refresh in
# EVERY browser); the service worker file too. Hashed/versioned assets can
# cache hard.

# Security headers on every response.
#
# The Content-Security-Policy below is deliberately in REPORT-ONLY mode: it does
# not block anything yet, it only logs violations to the browser console. The
# allowlist was derived from every external host the CLIENT code touches (GIS +
# gapi for sign-in, cdnjs for html2pdf, Google Fonts, the direct Gemini call on
# the bring-your-own-key path, Clarity analytics). Once the app has been used for
# a full round — sign in, build a quote, export a PDF, open a share link — with a
# clean console, rename the header to `Content-Security-Policy` to ENFORCE it.
#
# Note `script-src` must keep 'unsafe-inline': the app is built on inline
# onclick= handlers, so removing it would be a large refactor. The real value
# here is `connect-src` — it stops an injected script from exfiltrating
# localStorage (tokens, client data) to an attacker's domain.
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: SAMEORIGIN
  Referrer-Policy: strict-origin-when-cross-origin
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Permissions-Policy: geolocation=(), microphone=(), camera=()
  Content-Security-Policy-Report-Only: default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com https://accounts.google.com https://apis.google.com https://www.clarity.ms; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: blob: https:; connect-src 'self' https://generativelanguage.googleapis.com https://www.googleapis.com https://oauth2.googleapis.com https://accounts.google.com https://www.clarity.ms https://*.clarity.ms; frame-src https://accounts.google.com https://content.googleapis.com; object-src 'none'; base-uri 'self'; form-action 'self'

/
  Cache-Control: no-cache

/*.html
  Cache-Control: no-cache

/sale/
  Cache-Control: no-cache

/sale/index.html
  Cache-Control: no-cache

/sale/sw.js
  Cache-Control: no-cache

/zerem/
  Cache-Control: no-cache

/q/
  Cache-Control: no-cache

/sale/styles.css
  Cache-Control: no-cache

/sale/app.js
  Cache-Control: no-cache

/styles.css
  Cache-Control: no-cache

/app.js
  Cache-Control: no-cache

/assistant.js
  Cache-Control: no-cache

/ask/
  Cache-Control: no-cache

/ask/index.html
  Cache-Control: no-cache

/assets/listcards.js
  Cache-Control: no-cache
