# ANALYTICS — יומן מדידה ושיפור (מנוהל ע"י קלוד)

מטרה: שיפור שיטתי של האתר על בסיס דאטה אמיתית — לא תחושות בטן, ולא שינויים בלי מספיק ראיות.

## חוקי העבודה (סוכם עם סתיו, 05/08/2026)

1. **קצב:** רוטינה אוטומטית מושכת נתוני Clarity כל 3 ימים (ה-API מחזיר עד 3 ימים אחורה,
   כך שאין חורים ברצף).
2. **סף פעולה:** אין שינויים באתר עד שנצברו מספיק נתונים — לפחות ~150 סשנים מאז
   השינוי האחרון **וגם** דפוס ברור וחוזר (למשל: rage clicks באותו אלמנט, נטישה
   עקבית באותה נקודה). אחרת: רק תיעוד, בלי לגעת.
3. **שינוי אחד בכל פעם** — כדי שאפשר יהיה לייחס שיפור/נסיגה לשינוי ספציפי.
4. **כל שינוי נרשם כאן** עם תאריך, ההשערה, והמדד שאמור לזוז.
5. שינויים גדולים (עיצוב מחדש, מחיקת תוכן, שינוי מחירים) — רק באישור סתיו.

## מדדים במעקב

sessions · bounce/quickback · זמן עמוד ממוצע · עומק גלילה · rage/dead clicks ·
דפים פופולריים · מקורות תנועה · המרות (הגעה ל-thanks.html, מעבר לוואטסאפ, כניסה לזרם).

## סטטוס חיבורים

- [ ] Clarity — **הצינור נשבר ב-31/08 ותוקן בענף**: סשן אבטחה נעל את GET /api/clarity
      מאחורי הזדהות אדמין (נכון — נתוני התנועה היו חשופים לכולם), אבל זה ניתק את
      משיכת ה-GitHub Actions. תוקן בענף: ה-Workflow מזדהה עכשיו עם GitHub OIDC
      (חתום, מוצמד לרפו הזה, בלי סודות) והממסר מאמת את החתימה. ממתין לדיפלוי ל-main.
      הטוקן עצמו בשרת (KV או CLARITY_API_TOKEN) — עבד עד 28/08.
- [ ] GA4 — ממתין ליצירת נכס ומזהה G-XXXX (הקוד באתר מוכן).

## כיסוי מדידה נוכחי

Clarity פעיל על: כל דפי התדמית (10 עמודים) + דף הנחיתה של זרם (מ-05/08/2026).
לא פעיל בכוונה על: /sale (נתוני לקוחות של משתמשים), /ask (תוכן שיחות).

---

## יומן

### 31/08/2026 — סריקה #9 — הצינור נשבר ותוקן
- אבחון: סשן אבטחה מקביל נעל את GET /api/clarity מאחורי הזדהות אדמין (מוצדק —
  נתוני התנועה היו ציבוריים), ובכך נותק ה-GitHub Actions. אין רשומות 28/08-31/08.
- תיקון (בענף, ממתין לדיפלוי): ה-Workflow מזדהה עם GitHub OIDC חתום ומוצמד-רפו;
  הממסר מאמת חתימה מול JWKS. בלי סודות, בלי צעדים מסתיו, והציבור נשאר בחוץ.
- היגיינת מדידה: Clarity לא נטען יותר על localhost (רשומת 28/08 13:37 הכילה 3
  "סשנים" של שרת פיתוח). מוזג main המעודכן (כולל track.js — מונה עצמאי חדש
  של סשן מקביל, מקור הצלבה טוב).
- נתונים: אין חדשים; הידוע האחרון — אפס תנועה אמיתית. אין שינויים באתר (חוק 2).

### 28/08/2026 — סריקה #8
- הריצה המתוזמנת של היום התעכבה (עיכובי schedule של GitHub; תיקון ה-03:30 עדיין
  בענף) — הפעלתי משיכה ידנית וקיבלתי רשומה טרייה 06:22.
- חלון 25-28/08: **0 סשנים**, שבוע שני ברציפות של אפס תנועה. אין שינויים (חוק 2).
- ממתין למהלך הפצה. המערכת יציבה ואוטונומית.

### 25/08/2026 — סריקה #7 — הצינור חי!
- הטוקן נשמר בשרת: רשומת 25/08 הגיעה דרך המשיכה המתוזמנת, בלי התערבות. מהיום
  המערכת אוטונומית לגמרי.
- הנתונים (חלון 22-25/08): **0 סשנים**. אין תנועה לאתר בכלל בחלון הזה (רשומת 14/08
  עם 5 הסשנים הייתה ביקורי בדיקה). אפס חיכוך = אין מה לנתח.
- 0≪150 → אין שינויים (חוק 2). המסקנה מסריקה #4 מתחזקת: הצוואר הוא הפצה, לא UX.
  הצעד הבא שראוי לתשומת לב הוא מהלך ה-Growth (דחיפת /ask/ לקבוצות), לא שינויי אתר.

### 22/08/2026 — סריקה #6
- ריצת 22/08 (05:30) — עדיין `token-not-set`; אין רשומות חדשות, אין שינויים (חוק 2).
- מדיניות נדנוד: מכאן והלאה תזכורת טוקן לסתיו רק אחת לשבוע (הבאה: לא לפני 29/08),
  שאר הסריקות מתועדות ביומן בלבד.

### 19/08/2026 — סריקה #5 — אבחון: הצינור עדיין יבש
- הריצות המתוזמנות של 16/08 ו-19/08 קיבלו שוב `token-not-set`. חקירה העלתה:
  הרשומה מ-14/08 לא הגיעה דרך הצינור — היא הוזרקה ידנית ע"י סשן קלוד אחר (כחלק
  מקומיט פיצ'ר הפעמון), כנראה עם טוקן שסתיו נתן לו בצ'אט. הטוקן מעולם לא נשמר
  בשרת — לא ב-KV (הכרטיס באדמין) ולא במשתנה הסביבה CLARITY_API_TOKEN שהסשן
  ההוא הוסיף כ-fallback.
- כלומר: עדיין חסר אותו צעד יחיד — להדביק את הטוקן בכרטיס "חיבור Clarity" באדמין.
- אין נתונים חדשים → אין שינויים (חוק 2). הרשומה היחידה (5 סשנים, ביקורי בדיקה)
  לא מספיקה לכלום.

### 16/08/2026 — סריקה #4 — נתונים ראשונים!
- רשומה ראשונה (חלון 12-14/08): **5 סשנים, משתמש יחיד**, מכשיר PC, דפדפן "Other",
  דף פופולרי "https://Electron", ללא מקור תנועה, activeTime 0 — כמעט ודאי ביקורי
  בדיקה שלנו/של סתיו, לא גולשים אמיתיים. אפס חיכוך (rage/dead clicks, שגיאות JS: 0).
- **מסקנה:** 5≪150 → אין שינויים (חוק 2). התובנה האמיתית: הצוואר כרגע הוא
  הפצה/תנועה, לא UX — אין עדיין מה לאופטם כשאין מבקרים. ההמלצה בבקלוג Growth
  (דחיפת /ask/ לקבוצות וואטסאפ) היא המנוף, לא שינויי עיצוב.
- תפעולי: תזמון ה-Workflow הוקדם ל-03:30 UTC כדי שירוץ בבטחה לפני הסריקה
  (בפועל GitHub מריץ באיחור של עד ~80 דק').

### 13/08/2026 — סריקה #3
- ה-Workflow רץ בתזמון (06:20 UTC) והממסר עדיין מחזיר `token-not-set` — הטוקן טרם
  הודבק באדמין. הצינור עצמו תקין. ללא נתונים — אין שינויים (חוק 2).

### 10/08/2026 — סריקה #2
- הצינור אומת מקצה לקצה בפרודקשן: ה-Workflow רץ בתזמון (06:12 UTC), הגיע לממסר
  באתר החי וקיבל תשובה תקינה — `token-not-set`. כל החוליות עובדות; חסר רק הטוקן
  של סתיו בכרטיס "חיבור Clarity" באדמין. ללא נתונים — אין שינויים (חוק 2).
### 14/08/2026 — רשומת עומק (נכתבה ע"י סשן מקביל)

הטוקן עבד, ה-API נענה, והצילום הראשון נשמר ב-`data/clarity/latest.json`.
חלון: 3 ימים אחורה (זה כל מה שה-API נותן), כל האתר התדמיתי + דף הנחיתה של זרם.

מה שחזר, במלואו:

| מדד | ערך |
|---|---|
| סשנים | 5 |
| משתמשים נבדלים | **1** |
| בוטים | 0 |
| דפדפן | "Other" (לא כרום/ספארי/פיירפוקס) |
| מקור הסשן (URL) | `https://Electron` — כולם |
| Referrer | ריק בכל 5 |
| מכשיר / מערכת / מדינה | PC · Windows · ישראל |
| זמן פעיל | 0 שניות (מתוך 573 שניות פתוחות) |
| עומק גלילה ממוצע | 8.41% |
| Rage / dead clicks / שגיאות | 0 בכולם |

**המסקנה, בלי לרכך:** אלה לא מבקרים. משתמש אחד, דפדפן משובץ בתוך אפליקציית דסקטופ
(Electron), אפס זמן פעיל, אפס referrer — זו תצוגה מקדימה של האתר מתוך אפליקציה,
לא תנועה. כלומר בשלושת הימים האחרונים **אף אדם אמיתי לא נכנס לאתר**.

מה זה אומר לעבודה: חוק 2 (אין שינויים מתחת ל-150 סשנים) עומד בתוקפו — אבל הסיבה
השתנתה. אנחנו לא מחכים לנתונים כדי לשפר UX; אין UX לשפר כי אין מי שיראה אותו.
הצוואר הוא הפצה, לא ממשק. מפות חום יתחילו להיות שוות משהו רק אחרי שמישהו ישלח
קישור לקבוצה של חשמלאים.

סייגים כדי לא להטעות: (1) זה חלון של 3 ימים, לא היסטוריית האתר; (2) Clarity לא
מותקן בכוונה על `/sale` ו-`/ask`, אז שימוש במערכת עצמה לא נספר כאן; (3) המונה
העצמאי שלנו (`track.js` → פאנל האדמין) הוא מקור נפרד ולא חסים לאד-בלוקרים —
שם כדאי להצליב.

### 07/08/2026 — סריקה #1
- אין עדיין גישה לנתונים: `CLARITY_TOKEN` לא מוגדר בסביבה, ו-www.clarity.ms עדיין חסום
  במדיניות הרשת. שני הצעדים אצל סתיו. ללא נתונים — אין שינויים (לפי חוק 2).

### 05/08/2026 — הקמה
- הוקמה הרוטינה (כל 3 ימים). אין עדיין גישת API — ממתין לשני צעדי החיבור של סתיו.
- שינויים שנעשו היום (נקודת ייחוס למדידה): שורת עוגיות, דף תודה לטופס, דף 404,
  עמודי פרטיות ונגישות, Clarity נוסף לדף הנחיתה של זרם.
