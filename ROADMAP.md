# ZEREM (זרם) — מפת דרכים ורעיונות

מסמך עבודה חי. מה שסומן ✅ כבר באוויר; השאר לפי סדר עדיפות משוער.

## גל 0 — מרכז הכובד עובר לאפיון (החלטת סתיו, 11.8.2026)
הבעיה שזוהתה: הפלטפורמה דוחפת לתמחור מהר מדי ולכן מפספסים דברים. השורש בקוד —
פרומפט התכנון מורה במפורש "שאל עד 2 שאלות, לא יותר" ו"ספק מיד את הרשימה המלאה"
(`sale/app.js`, `getPlanningSystemInstruction`).

**הוחלט (סתיו בחר אפשרות א'):** הפיבוט הוא **פנימה בלבד** — שלב האפיון נהיה עמוק
ומחייב ומסתיים ב"כרטיס אפיון" מאושר, ורק אז נפתח כפתור **"תמחר פרויקט זה"**.
השיווק ודף הנחיתה ממשיכים למכור הצעת מחיר ("הצעה שלא מפספסת כלום"). אם האפיון
יוכיח את עצמו כערך שמדברים עליו — נשקול להעלות אותו גם לשלט.

- ✅ מודל כיסוי בקוד (`sale/coverage.js`) — 4 צ'קליסטים, 14 שדות ו-5 חובה בכל אחד, נכתבו בסבב אדוורסרי
- ✅ כרטיס אפיון בממשק: ה-AI ממלא מראש (מתויג "הצעה"), המשתמש מתקן בצ'יפים, "לא יודע" = הנחה כתובה
- ✅ "תמחר פרויקט זה" כשער נעול + דלת מילוט "דלג ותמחר עכשיו" שרושמת הכל כהנחות
- ✅ הנחות ו"מה לא כלול" נכנסים אוטומטית לתנאי ההצעה — בקוד, לא דרך סוכן הניסוח
- ✅ "פקודת עבודה לשטח" (הדפסה) — האפיון + חומרים + כלים + תשומת לב. מסמך פנימי, בלי BOM ללקוח
- ✅ ערכות ציוד פר-משפחת-עבודה בשרת (`functions/api/_electrical_kit.js`) — 12 משפחות, 986 פריטים,
  מוזרקות לסוכן האפיון לפי סוג העבודה. מסמך לעיון: `ELECTRICAL_KIT.md`
- ✅ /ask/ מסיים ב"על מה המחיר נשען ומה לא נבדק" + כפתור לאפיון מלא
- ✅ **9 סוגי עבודה** בצ'קליסטים (גל 2 הוסיף סולארי, בדיקה ודוח, תקלה, תאורה, הארקות).
  זיהוי אוטומטי של סוג העבודה מהתיאור — 10/10 בבדיקה על ניסוחים אמיתיים
- ✅ שדה "איפה הנכס ואיך מגיעים" נוסף לכל צ'קליסט שלא כיסה אותו — חמש ביקורות השטח
  העלו את אותו פער בדיוק: נסיעה, חניה וקומה בלי מעלית הן שעות שאף אחד לא מתמחר
- ✅ נגישות: אפס כשלי ניגודיות ב-9 סוגי עבודה × 3 ערכות נושא × 3 מצבי שורה.
  תוקן: טקסט משני שירד ל-4.35:1 בערכה הבהירה, מספרים זהובים ב-1.7:1, וכפתור
  "תמחר" בירוק של 2.15:1. צ'יפים 44px במגע, focus נראה, ARIA לסרגל ההתקדמות,
  ו"למה שואלים" הפך לכפתור אמיתי במקום tooltip שלא נגיש באצבע
- ✅ הכרטיס מציג רק שדות חובה (5) + מה שכבר נענה; השאר מאחורי "עוד N שדות".
  במובייל זה הוריד אותו מ-4,767px ל-2,161px
- [ ] סתיו: לעבור על ההחלטות ו"מה הכי שוכחים" ב-`ELECTRICAL_KIT.md` ולתקן
- [ ] גל 2: להריץ את סבב המיזוג האדוורסרי (הביקורות מוכנות ב-journal של
  `wf_400a3197-582`; סוכני המיזוג נפלו על מגבלת שימוש). הטיוטות באוויר ותקינות —
  זה שיפור, לא חוסם
- [ ] צ'קליסטים לשאר המקצועות (אינסטלציה, מיזוג, שיפוצים) — היום נופלים לגנרי

## גל 3ג — הלוגו חוזר, הרוחב גדל, התעודה מתרווחת (21.8.2026)

- ✅ **הלוקאפ מהאתר הישן**: SJ מעל "הנדסת חשמל", והטבעת המקווקוות מסתובבת סביב
  הברק — 24 שניות לסיבוב, כלומר נראה רק למי שמסתכל. `prefers-reduced-motion`
  מכבד. אותה שפה כמו טבעת התעודה, אז זה נקרא כמשפחה אחת ולא כאפקט.
- ✅ **רוחב העמוד**: 1080px → 1240px, ומעל 1700px → 1400px. במסך גדול או מוגדל
  הצדדים היו ריקים והעמוד נראה כאילו הודפס על דף צר מהמסך. גושי הטקסט שומרים על
  התקרות שלהם ב-ch, אז השורות לא מתארכות — רק הרשתות מתרחבות, ושם המקום שווה.
- ✅ **התעודה מתרווחת** — כמו תעודה מודפסת: 740px גובה מתוך 844 בטלפון, עדיין
  בלי גלילה. מקום לשתי דמויות מצדיעות שמור (`.sj-mil-salutes`), האיור עצמו
  ממתין לקובץ מסתיו.
- ✅ **התמונה של "מי אנחנו" ירדה** — גם מהפס בעמוד הבית וגם מהירו של /about.

## גל 3ב — סריקת באגים על כל האתר + זרם (20.8.2026)

- ✅ **הטקסט האפור על התמונה**: `.hero .lede` הוגדר *אחרי* `.hero-photo .lede` באותה
  ספציפיות, ולכן צבע הטקסט המשני צבע מחדש את הכיתוב מעל התצלום. בלוק ההיפוך
  הועבר לאחרי כללי הבסיס.
- ✅ **בודק ניגודיות אוטומטי** (`scratchpad/contrast.mjs`): עובר על כל אלמנט טקסט
  בכל עמוד, בשתי הערכות, מחשב את הרקע האפקטיבי ואת יחס הניגודיות מול סף WCAG
  לפי גודל וכובד הפונט; מעל תצלום הכלל הוא פשוט "הטקסט חייב להיות בהיר".
  מה שהוא מצא ותוקן: `--text-3` נפל ל-4.04:1 על המשטח הבהיר ביותר (הפוטר של כל
  עמוד) אחרי מעבר הפלטה — הועמק ל-#5D6A7C; כפתורי וואטסאפ היו 2.86:1 (לבן על
  ירוק המותג) — נוסף `--whatsapp-btn` כהה יותר, בדיוק כמו ההפרדה הקיימת בין
  `--ok-text` ל-`--ok-btn`; כפתור ראשי ב-/checkups היה 2.04:1 (שחור על נייבי).
- ✅ **הצ'אט ב-/sale רץ בלי עיצוב**: assistant.js פועל גם באפליקציה, אבל ה-CSS שלו
  ישב ב-styles.css ש-/sale לא טוענת. עבר ל-`assets/ui.css` — השכבה המשותפת
  היחידה שגם האתר וגם האפליקציה טוענים.
- ✅ **האפליקציה נשארה כתומה**: `sale/css/shell.css` מחזיקה עותק שני של הפלטה
  (האפליקציה מחליפה ערכה לפי class על body ולא לפי [data-theme]). סונכרן, עם
  הערה שהערכים חייבים לעקוב אחרי tokens.css.
- ✅ **זרם קיבל את אותו טיפול**: פס פתיחה כהה עם השרטוט במקום התצלום — תוכנית
  חשמל אומרת על תוכנה מה שתצלום תורנים לא יגיד. הטוקנים מוגדרים מחדש *על* הפס,
  כך שה-SVG (שצובע את עצמו ב-var(--accent)) וכרטיסי השלבים עוקבים בלי סלקטור נוסף.
- ✅ 15 עמודים × 2 ערכות: אפס גלישה, אפס SVG מנופח, אפס פקד עם ברירת מחדל של
  הדפדפן, אפס שגיאת JS, אפס בקשה שנכשלה.

## גל 3א — האתר מקבל בחזרה תמונה (20.8.2026)

הביקורת של סתיו: "האתר החדש נראה כמו מאמר ב-WORD, אני מתגעגע לישן". האבחנה,
אחרי השוואה של שתי הגרסאות זו מול זו: V3 הסירה **כל** דימוי מהאתר, וכל אזור
קיבל את אותו רקע בדיוק — עמוד שלם של טקסט על רקע אחיד הוא ההגדרה של מסמך.
מה שלא חוזר מ-V2: אימוג'י כאייקונים, ערימת הקישוטים, והרכיבים המפוברקים
(ראה V2-RECON §3) — שם היה קישוט, לא חום.

- ✅ הירו של כל עמוד הוא תמונה על מסך מלא (`.hero-photo`), עם שני סקרימים
  ורשת שרטוט עדינה. הפס הזה **לא** עוקב אחרי ערכת הנושא של המבקר בכוונה:
  תצלום לילה עם טקסט לבן בשתי הערכות, כי תמונה שמתבהרת ומתכהה לפי המכשיר
  נראית כמו תקלה. זה החריג היחיד לכלל הטוקנים.
- ✅ פס מספרים כהה מתחת להירו — 500+ / 15+ / 4.9. אותם נתונים היו קודם בשורה
  אפורה קטנה מתחת לכפתורים, כלומר בתור הערת שוליים.
- ✅ פס תמונה באמצע העמוד ("מי אנחנו") — עשרה קטעי טקסט ברצף היו הבעיה.
- ✅ `.section-alt` עבר ל-`--surface-2`: ההבדל מ-`--bg` היה שערה בערכה הבהירה,
  ולכן כל הקטעים נראו כמו אותו קטע.
- [ ] גל הבא (רק כשיהיו נכסים): חיתוך שרטוט AutoCAD אמיתי כרקע ההירו במקום
  התצלום, וצילומי שטח בטיפול דו-גוני.

## גל 0ב — אזור מנהל: תנועה ומפות חום (11.8.2026)
- ✅ מונה תנועה משלנו (`track.js` + `functions/api/analytics.js`) — עמודות נפרדות לאתר ולזרם,
  ללא קוקיות וללא צד שלישי, לא ניתן לחסימה ע"י אד-בלוקר
- ✅ נטרול כניסות: התחברות כאדמין מסמנת את המכשיר אוטומטית; בכל דפדפן אחר `?notrack=1` פעם אחת;
  אוטומציה ו-headless מסוננים; בוטים נספרים בנפרד ולא מתערבבים במספר האמיתי
- ✅ מפות חום: Clarity Data Export API בשרת (המפתח לא מגיע לדפדפן), עם צילום יומי ל-KV
  כדי לקבל מגמה ארוכה מ-3 הימים ש-Clarity נותנת
- ✅ "מה השתנה השבוע" — מסקנות מחושבות בכל פתיחה של הפאנל (לא job שבועי שיכול להישבר בשקט)
- ✅ (20/08) הכרטיס נקרא עכשיו בשנייה: ארבעה מספרים גדולים — היום / מתחילת השבוע / מתחילת
  החודש / מתחילת השנה, כל אחד מול התקופה המקבילה, ועמודה לכל חודש ב-12 החודשים האחרונים
  (`summary=1` ב-`/api/analytics`, עם רולאפ חודשי ב-KV כדי שהשנה לא תעלה 365 קריאות).
  הפירוט (דפים, מקורות, בוטים) ירד לתוך `<details>`, ומפת החום פותחת בשורת שיפוט אחת:
  יש/אין מספיק תנועה כדי שהיא תגיד משהו
- ✅ רשימת המשתמשים הרשומים כבר קיימת ועובדת (`functions/api/admin-users.js`)
- ✅ הטוקן נוצר ונבדק מול ה-API (14/08) — הצילום הראשון שמור ב-`data/clarity/`
- [ ] **פעולה של סתיו (דקה אחת):** להדביק את הטוקן באחד משני המקומות — כרטיס
  "חיבור Clarity" בפאנל האדמין של זרם, **או** Cloudflare Pages → Variables and
  secrets → `CLARITY_API_TOKEN`. הממסר מקבל את שניהם. בלי זה המשיכה האוטומטית
  כל 3 ימים לא רצה, וההיסטוריה לא נצברת

## גל 0ג — GEO: לגרום למודלי AI להפנות לאתר (11.8.2026)
- ✅ `pricing-guide.html` — "כמה עולה עבודת חשמל": טווחים אמיתיים, מה מזיז את המחיר, מה לא כלול,
  וטבלאות אגרות חח"י המלאות (מגורים, מסחר, חיבור חדש). FAQPage + Article בנתונים מובנים
- ✅ `llms.txt` — פרק מחירים שלם ומצוטט, כולל הזמנה מפורשת לצטט עם קישור
- ✅ `robots.txt` — אישור מפורש לכל זחלני ה-AI (GPTBot, ClaudeBot, PerplexityBot, Google-Extended ועוד)
- ✅ קישורים פנימיים מכל דפי האתר + sitemap
- [ ] אחרי חודש: לבדוק אם מודלים מצטטים את הדף (לשאול כמה מודלים "כמה עולה החלפת לוח בישראל")

### שרטוט אוטומטי של הפרויקט (בקשת סתיו, 11.8.2026 — "אחרי שתסיים את השאר")
כפתור בסגנון NotebookLM שמג'נרט **ציור משוער של העבודה** מתוך האפיון.
דרישות מדויקות כפי שנמסרו: שחור-לבן, **חד-קווי**, מינימלי ככל האפשר בטוקנים,
במאמץ חישוב ובזמן — אבל **מדויק וברור**. חכם לפי סוג העבודה, כולל מבט-על ומבט-צד
כשצריך:
- יציאה ממבנה והליכה על גדר עם כבל → מבט על + מבט צד
- לוח חשמל → קיר עם לוח, ואולי שבירת בטון ויציאה לתעלת כבלים
- עמדת טעינה → איפה מרימים את האבנים המשתלבות
- שקע CEE חיצוני → חייב להגיד שצריך חציבה **או** קופסת חיבורים (סתיו צירף תמונת
  שדה: קופסת Photex IP-44 מעל שקע CEE כחול על קיר אבן) — ולשרטט את זה

הוראת סתיו: "תנסה לעשות את זה ואם לא יצא טוב נרד מזה."

## גל 1 — חשבוניות (מתחיל ביום ראשון)
- [ ] קבלת API של SmartBee ופיילוט על החשבון של סתיו.
- [ ] **מגבלה: חשבונית עד ₪5,000** — מעל זה נדרשת אסמכתא מרשות המיסים; המערכת תחסום/תתריע.
- [ ] ארכיטקטורה: שכבת ספקים גנרית (כמו שכבת ה-AI) — SmartBee ראשון, אחר כך חשבונית ירוקה / iCount וכו'.
- [ ] כל משתמש מחבר את חשבון החשבוניות *שלו* (מפתח אישי בהגדרות).
- [ ] **אזור נפרד לגמרי במערכת**: הניווט יתחלק ל-3 עולמות — הגדרות | פרויקטים | ניהול חשבונות. הרבה עסקים קטנים עובדים עם רו"ח — עולם החשבונות חייב להיות מופרד ואופציונלי.
- [ ] הזרימה: הצעה בסטטוס "הושלם" ← כפתור "הפק חשבונית" ← סטטוס "שולם".

## גל 2 — ספקים והשוואת מחירים (החזון הגדול)
- [ ] **מודול "ניהול ספקים" באדמין בלבד (מפרט מסתיו, 04/07)**: לכל ספק — כרטיס פרטים (שם, כתובת, טלפון, מייל, קישור לאתר) + העלאת כל המוצרים והמחירים שלו (ייבוא CSV/הדבקה כמו במאגר). המאגרים נשמרים פר-ספק (KV `supplier:<id>`), ובהמשך שלב התמחור משווה ביניהם. לתכנן מבנה לפני קוד.
- [ ] שותפויות ספקים: ארכה, חשמל ישיר ועוד — קבלת מחירונים רשמיים; מתחילים עם הספק השכונתי של סתיו (רשימת מוצרים ב-PDF ← ייבוא).
- [ ] שלב "תמחר" משווה בין ספקים ומציג מאיפה הכי משתלם לקנות.
- [ ] כפתור "הוסף את כל המוצרים מאתר X" (הרחבת הסריקה לזחילה מלאה — דרך שותפות/פיד, לא scraping).
- [ ] "מצא את הספק הקרוב אליך" + כתובת/עיר מהמשתמש (דרך Google) + זמן נסיעה (Waze/Maps API) — עתיד רחוק.
- ✅ CTA בדף הנחיתה: "אתה ספק? הצטרף" (וואטסאפ/מייל) + טופס "מכיר ספק? שם + עיר" (מגיע למייל של סתיו).
- [ ] לוגואים של ספקים שותפים בדף התדמית של זרם.

## גל 3 — ניהול המאגר (אדמין)
- ✅ כפתור "נתח וסדר את המאגר עם AI" בפאנל האדמין — מאחד וריאציות זניחות, שומר וריאציות הנדסיות, מסיר זבל.
- ✅ סביבת ניהול מאגרים באדמין: 1️⃣ העלאה (הדבקה/CSV) ← 2️⃣ "נתח את המאגר" ← 3️⃣ "המאגר של כולם" (רשימה + פרסום).
- ✅ זיהוי אוטומטי של שינוי במאגר ← התראה "זוהה שינוי — נתח ופרסם".
- [ ] קטגוריות: הדף יתחלף בין חשמל / אינסטלציה / מיזוג וכו' (הכנה להתרחבות לענפים).
- [ ] **רשימת משתמשים באדמין (סתיו, 04/07 — "צריך לזכור")**: "ניהול מסלולים" מציג היום רק חיפוש לפי אימייל; להוסיף רשימה של כל המשתמשים הרשומים (KV list על `user:*`) עם המסלול של כל אחד. הערה: משתמש מופיע ב-KV רק אחרי שסינכרן נתונים לפחות פעם אחת (sj.eng.business@gmail.com יופיע אחרי כניסה+שמירה ראשונה).

### מדיניות איחוד וריאציות (הוחלט)
- מאחדים כשההבדל זניח מחירית (~עד 7%) ולא משנה בחירה הנדסית: "וו תליה פתוח/סגור" ← "וו תליה" במחיר הגבוה.
- לא מאחדים כשהווריאציה היא החלטה הנדסית: לוח 12/18/24 מודול, חתך כבל, אמפראז'.
- ביניים (עוגן ב-4 אורכים, פער 3.2–4.2): מאחדים לשם גנרי עם טווח או מחיר ממוצע-גבוה — ל-AI זה מספיק לתמחור; הבחירה המדויקת נעשית בשטח.
- שינוי בחירה של המשתמש (לוח 24 ← 18) נעשה **דרך הצ'אט** ("תחליף ל-18 מודול") — בלי UI ייעודי בשלב זה.

## גל 4 — חוויית לקוח קצה
- ✅ חתימת לקוח על המסך (עכבר/אצבע) מוטבעת ב-PDF.
- ✅ תזכורות מעקב (הצעה שנשלחה / תשלום שממתין) עם מייל/וואטסאפ בקליק.
- 🔶 ארכיון לקוח: השלב הראשון בנוי — **"קישור ללקוח"**: כל הצעה מקבלת קישור קבוע (sj-eng.co.il/q/?t=...) שהלקוח פותח בדפדפן, כולל חתימה אם נחתמה וכפתורי חיוג/וואטסאפ לעסק. נשמר על הפרויקט. השלב הבא: דף ארכיון שמרכז את כל ההצעות של אותו לקוח.
- [ ] הודעות וואטסאפ יזומות דרך WhatsApp Business API — נדחה (עלות פר הודעה + חיבור API). מה שקיים היום: קישורי wa.me חינמיים בקליק.

## נדחה / הוחלט שלא
- עדכוני מחירי שוק ("הנחושת קפצה 15%") — לא מקצועי לנחש, יקר לתחזק אמת; מחירי ברגים לא זזים. ירד.
- סליקת תשלומים בעמוד השירותים — ירד; הכיוון הוא חשבוניות, לא סליקה.

## מהלך 3 — ליטוש (מפרט שאושר + תוספות מסתיו)
- ✅ 3 תבניות עיצוב מוכנות להצעה (קלאסית / מודרנית / מינימלית) — בחירה בלבד, בעורך ההצעה.
- ✅ **מעצב בלוקים ל-PDF**: כפתור "עיצוב ידני" (במחשב) — גרירה/סדר של בלוקים (כותרת/שם/סעיפים/חתימה; שורת הסיכום קבועה למטה), יישור+גודל+הדגשה+קו תחתון לכל בלוק, וכפתור "הצעה באנגלית" שהופך ל-LTR. נשמר ב-settings.quoteLayout, מוחל על ההצעה החיה וב-PDF. בנייד — תבניות במקום. (הערה: שינוי גודל בלוק מוצג מדויק בתצוגה; ב-PDF ה-zoom של html2canvas עשוי לסטות מעט — סדר/יישור/הדגשה/אנגלית מדויקים ב-PDF.)
- ✅ העלאת תמונה בצ'אט התכנון/תמחור — ה-AI רואה את השטח (Gemini multimodal, עד 4 תמונות, thumbnails בבועה).
- ✅ מפתח API נפרד למשתמשים משלמים (GEMINI_API_KEY_PAID) — בידוד עלויות (chat.js→_ai.js paidTier). **פעולה של סתיו: להוסיף את משתנה הסביבה ב-Cloudflare כשרוצים את ההפרדה.**
- ✅ Onboarding (מסך פתיחה 4 שלבים) + שער-רך לפרטי עסק אחרי פרויקט ראשון (ניתן לדילוג).
- ✅ דף ארכיון פר-לקוח ("ארכיון לקוחות") — כל ההצעות מקובצות לפי לקוח, סטטוסים, סכומים והעתקת קישור.
  **הערה (15/08/2026): ה-✅ הזה היה שקר במשך זמן מה.** הפונקציה, ה-CSS והעוזרים היו כתובים
  ומוטמעים — אבל הלשונית והפאנל שמארחים אותם מעולם לא נכנסו ל-HTML, כך שאי אפשר היה להגיע
  לדף. התגלה כשחיברנו פרויקטים ללקוחות; הפאנל הוחזר ועכשיו הוא באמת באוויר.
- ✅ שיוך פרויקט ללקוח אמיתי (15/08/2026) — צ'יפ "לקוח" ליד הקטגוריה בכרטיס הפרויקט.
  הארכיון מקבץ לפי הזהות ולא לפי הטקסט (שתי כתיבות של אותו שם = לקוח אחד), והטלפון
  והמייל שהתזכורות שולחות אליהם מגיעים מרשומת הלקוח במקום להיות מוקלדים שוב בכל פרויקט.

## גל 6 — סטטיסטיקת תמחור קהילתית (חפיר הנתונים) — v4.18
- ✅ תשתית: איסוף אנונימי של מחיר-עבודה-בלבד בהורדת PDF (functions/api/stats.js), לפי {מקצוע, סוג-עבודה, חודש} — אפס PII של לקוחות. חציון+טווח (לא ממוצע משקר), מינימום 5 דגימות, גבולות שפיות, דדופ לפי quoteId, rate-limit. **תצוגה כבויה (config:statsLive) עד שסתיו מדליק** — נאספים נתונים בשקט מהיום.
- ✅ לשונית אדמין "ניהול סטטיסטיקות": סה"כ/החודש, טבלת חציון+טווח+דגימות לכל סוג עבודה, וכפתור "הצג לכולם". מצרפי בלבד — אין גישה להצעות בודדות של משתמשים (החלטה מודעת: הגנה משפטית על סתיו מפני החזקת PII של לקוחות זרים).
- ✅ בחירת תרומה בפרטי עסק: אנונימי (ברירת מחדל) / עם שם העסק / לא להשתתף.
- [ ] v2: תגיות פרמיה (עבודת לילה / הקפצת חירום / התמחות) לסינון — נאסף אחרי שיש נתוני בסיס.
- [ ] v2: סינון גיאוגרפי (אזור/עיר) כשיש מסה קריטית.
- [ ] כשיש מספיק נתונים — סתיו מדליק את התצוגה מהאדמין.
- [ ] Onboarding: מסך התרשמות בכניסה ראשונה שמסביר את הזרימה.
- [ ] שער-רך לפרטי עסק: ביצירת פרויקט ראשון / ייצוא PDF ראשון — הצעה למלא פרטי עסק ולוגו, **עם דילוג**; מותר להוציא הצעה עם פרטים חסרים (לא כופים ח.פ. וכו').
- [ ] עיצוב מיקום פרטי העסק בהצעה: למעלה/למטה, ממורכז/צמוד לצד.
- [ ] מודל Gemini Pro כ"מודל מתקדם" למנויים (נכרך במהלך 2 — מיפוי בשרת).
- [ ] אחרי הליטוש: **שלב איכות המנגנון** — חידוד החוכמה/דיוק של הסוכנים בכל שלב (כולל סבב הפרומפטים מול ג'מיני).

## גל 5 — מודל עסקי (Freemium בשלוש שכבות) — ✅ בוצע (מהלך 2, v4.4)
- [x] תשתית: `tier:<email>` ב-KV, מגבלות בשרת (`config:tiers` — ניתן לעריכה מפאנל האדמין), /api/me + /api/tier, אכיפת AI לפי מסלול (אורח 10 / חינם 20 / Pro 150 / עסקי 300 ליום), 5 הצעות-ענן לחודש בחינמי (הצעה שישית נשמרת מקומית, PDF חופשי), נעילות: פרויקט רביעי, מאגר >10, דוחות (מוצג נעול), תזכורות (טיזר), קישור-לקוח; מסך שדרוג + כפתור וואטסאפ.
- [x] בחירת מודל בשמות גנריים: "מודל בסיסי" / "מודל מתקדם ⚡" (Gemini 2.5 Pro, ל-Pro ומעלה) — מיפוי בשרת בלבד.
- [x] הקשחת פרומפטים: כל הסוכנים הונחו לא לחשוף מודלים/ספקים/פרומפטים.
- [x] קרדיט "הופק באמצעות זרם ⚡" ב-PDF של חינם/אורח בלבד; חתימה חינם לכולם.
- [ ] תמחור סופי (79₪/149₪?) — המחיר במסך השדרוג כרגע "בקרוב"; לעדכן כשסתיו יחליט.
- [ ] סליקה/גבייה — שלב נפרד; בינתיים שדרוג ידני: פאנל אדמין → "ניהול מסלולים" (אימייל + מסלול).

## תזכורת לסתיו
- [ ] לקחת את כל הפרומפטים של הסוכנים (תכנון, תמחור, עוזר מערכת, עוזר ציבורי, חילוץ מחירים, ניתוח מאגר) לג'מיני — "אתה מכיר את עצמך הכי טוב" — ולבקש חידוד. הפרומפטים יושבים ב: sale/app.js (getPlanningSystemInstruction, getProfessionSystemInstruction, adminAnalyzeCatalog), functions/api/assistant.js, functions/api/scrape.js.

## עתיד רחוק
- [ ] אפליקציה נייטיבית (זה החזון הרחוק של המוצר).
- [ ] תרגום האתר והמערכת לשפות נוספות (אנגלית, רוסית, ערבית).
- [ ] דומיין גלובלי — בבדיקה (zerem.online פנוי; donedeal.ac מועמד; החלטה בהמשך).
